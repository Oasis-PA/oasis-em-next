# ====================
# CONFIGURAÇÃO DO BANCO DE DADOS
# ====================

# ⚠️ PROBLEMA DE CONEXÃO?
# Se você está recebendo erro "Can't reach database server", veja:
# - PROBLEMA_CONEXAO_BD.md (guia completo de solução)

# OPÇÃO 1: Conexão Direta (Porta 5432)
# Pode ser bloqueada por firewalls de rede corporativa/educacional
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.yyvjzgxyxgalnnwcjfqh.supabase.co:5432/postgres"

# OPÇÃO 2: Connection Pooling (Porta 6543) - RECOMENDADO se porta 5432 estiver bloqueada
# Acesse: Supabase Dashboard → Database → Connection Pooling
# DATABASE_URL="postgresql://postgres.yyvjzgxyxgalnnwcjfqh:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# OPÇÃO 3: Banco Local (Desenvolvimento)
# DATABASE_URL="postgresql://postgres:senha@localhost:5432/oasis_dev"

# ====================
# SEGREDOS JWT
# ====================

# Segredo para tokens de usuários comuns (mínimo 32 caracteres)
# Gerar novo: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET="your-super-secret-jwt-key-here-min-32-chars"

# Segredo para tokens de admin (mínimo 64 caracteres)
# Gerar novo: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
ADMIN_JWT_SECRET="your-super-secret-admin-jwt-key-here-min-64-chars"

# ====================
# AMBIENTE
# ====================

NODE_ENV="development"
# NODE_ENV="production"

# ====================
# CORS (Opcional para produção)
# ====================

# ALLOWED_ORIGIN="https://yourdomain.com"
